package OpenClosed.example2.before_apply_it;

public class ShapeDrawer {
    public void draw(Object shape) {
        if (shape instanceof Rectangle) {
            // draw it
        }
        if (shape instanceof Square) {
            // draw it
        }

    }
}
